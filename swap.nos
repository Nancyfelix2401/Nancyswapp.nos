#include<stdio.h>
int main()
{
int a,b,temp=0;
printf("enter 2 numbers:");
scanf("%d %d",&a,&b);
printf("value of numbers before swapping a=%d, b=%d\n",a,b);
temp=a;
a=b;
b=temp;
printf("value of numbers after swapping a=%d, b=%d\n", a,b);
return 0;
}
